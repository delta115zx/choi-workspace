import "./cct.css"; // 여기서 불러온다고 여기만 영향x, 이 사이트 전체에 영향

// id는 id, class는 className
const ChoiCSSThird = () => {
   return (
      <>
         <div id="a">ㅋㅋㅋ</div>
         <div className="b">ㅎㅎㅎ</div>
      </>
   );
};

export default ChoiCSSThird;
