# ── 직접 의존성 ───────────────────────────────────────────────
# Python 3.12 기준으로 검증됨

# AI 오케스트레이션 (Azure OpenAI 커넥터 포함)
semantic-kernel==1.40.0

# 환경 변수
python-dotenv==1.2.2

# Oracle DB 연결 (Oracle Instant Client 불필요)
oracledb==2.5.0

# API 서버
fastapi==0.115.12
uvicorn==0.34.3

# ── 핵심 전이 의존성 (버전 고정) ────────────────────────────
openai==2.26.0
azure-identity==1.25.2
aiohttp==3.13.3